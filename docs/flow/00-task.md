# Task Brief

**Requested by:** tikhonravova78@gmail.com
**Date:** 2026-09-08

## Ask (verbatim intent)
Build "Badminton: Clash of Rackets" — a **dashboard with statistics**. Repo and
local folder created; the actual product brief (what stats, whose data, who
uses it, how data gets in, what "done" looks like) has not been described yet.

## Known constraints / context
- (none captured yet — fill in below before starting the flow)

## To fill in before running the researcher
- What is this dashboard tracking? (e.g. badminton match results, player/team
  stats, tournament standings, personal training log)
- Who is the audience — a single user, a club, a public leaderboard?
- Where does the underlying data come from — manual entry, an import file,
  an existing spreadsheet, a live match-tracking flow?
- Which specific stats/metrics must be shown (win/loss record, head-to-head,
  rankings, streaks, points-per-game, etc.)?
- Any platform constraint (must run as a static site / needs to be hosted
  publicly / needs to persist data between sessions)?
- Any visual/branding direction implied by the "Clash of Rackets" name (e.g.
  competitive/arcade tone vs. plain data dashboard)?

This file is the seed input for the **researcher** agent. Do not edit it once the
flow starts; append findings to the numbered docs instead. Right now the flow
**should not be started** — the planner/researcher can't usefully proceed until
the section above is filled in.
